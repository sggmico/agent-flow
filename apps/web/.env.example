# ================================
# Web 应用私有环境变量模板
# ================================
#
# 使用说明：
# 1. 复制此文件为 .env.local
# 2. 填入实际配置值
# 3. .env.local 不会被提交到 Git
#
# 注意：此文件只包含 Web 应用专属配置
# 全局后端配置请查看根目录的 .env.example

# --------------------------------
# Client 暴露变量（构建期）
# --------------------------------
# GitHub 仓库路径（格式：owner/repo）
# 用于前端显示 GitHub Stars 等信息
NEXT_PUBLIC_GITHUB_REPO="yourusername/agent-flow"

# 应用 URL（生产环境需设置）
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# --------------------------------
# Web 专用服务端变量
# --------------------------------
# GitHub Personal Access Token（可选，用于提高 API 限制）
# 未认证：60次/小时，认证：5000次/小时
# 创建 Token：https://github.com/settings/tokens
# 只需要 public_repo 权限
GITHUB_TOKEN=""

# Web 应用端口（可选，默认 3000）
WEB_PORT=3000
